# ===== VARIABLES REQUERIDAS =====
# Sin estas, la aplicación no funciona

# Base de datos MongoDB (REQUERIDO)
MONGO_URI=mongodb://localhost:27017/db_name

# JWT Secret para autenticación (REQUERIDO)
JWT_SECRET=123456790abcdef1234567890abcdef

# ===== VARIABLES OPCIONALES =====
# Tienen valores por defecto

# Puerto del servidor (default: 3000)
PORT=3000

# CORS - Orígenes permitidos (default: localhost:5173,localhost:3000)
# Separadas por comas, sin espacios extras
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Entorno (development | production)
NODE_ENV=development

# URL API en producción (para Swagger UI)
# Si no está configurado, Swagger solo mostrará localhost
# Ejemplo: API_PROD_URL=https://api.example.com
# API_PROD_URL=